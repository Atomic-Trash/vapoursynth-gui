<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Standard Side Panel Container -->
    <Style x:Key="SidePanel" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBackground}"/>
        <Setter Property="CornerRadius" Value="{StaticResource RadiusM}"/>
        <Setter Property="Margin" Value="0"/>
    </Style>

    <!-- Center Content Panel -->
    <Style x:Key="CenterPanel" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBackground}"/>
        <Setter Property="CornerRadius" Value="{StaticResource RadiusM}"/>
        <Setter Property="Margin" Value="8,0"/>
    </Style>

    <!-- Panel Header Style (for collapsible sections) -->
    <Style x:Key="PanelHeader" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="HeaderBorder" Background="{StaticResource ElevatedBackground}"
                            CornerRadius="{StaticResource RadiusS}" Padding="12,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" VerticalAlignment="Center"/>
                            <!-- Down chevron when collapsed, Up chevron when expanded -->
                            <TextBlock x:Name="ChevronIcon" Grid.Column="1"
                                       Text="&#xE70D;" FontFamily="Segoe MDL2 Assets"
                                       FontSize="10" Foreground="{StaticResource TextSecondary}"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ChevronIcon" Property="Text" Value="&#xE70E;"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource PanelBackground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Panel Section Title -->
    <Style x:Key="PanelTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="Margin" Value="12,12,12,8"/>
    </Style>

    <!-- Panel Section Subtitle -->
    <Style x:Key="PanelSubtitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
        <Setter Property="Margin" Value="12,0,12,8"/>
    </Style>

    <!-- Panel Toolbar Style -->
    <Style x:Key="PanelToolbar" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ElevatedBackground}"/>
        <Setter Property="Padding" Value="12,8"/>
    </Style>

    <!-- Collapsible Panel Content -->
    <Style x:Key="CollapsibleContent" TargetType="Border">
        <Setter Property="Visibility" Value="Visible"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="False">
                <Setter Property="Visibility" Value="Collapsed"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Standard Page Toolbar -->
    <Style x:Key="PageToolbar" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ElevatedBackground}"/>
        <Setter Property="Padding" Value="16,10"/>
    </Style>

    <!-- Toolbar Separator -->
    <Style x:Key="ToolbarSeparator" TargetType="Rectangle">
        <Setter Property="Width" Value="1"/>
        <Setter Property="Fill" Value="{StaticResource BorderBrush}"/>
        <Setter Property="Margin" Value="12,4"/>
    </Style>

    <!-- Panel Toggle Button (for toolbar toggles like Marks, Keys, Mixer) -->
    <Style x:Key="PanelToggleButton" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="ToggleBorder" Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource RadiusS}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            <Setter TargetName="ToggleBorder" Property="Background" Value="{StaticResource PanelBackground}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            <Setter TargetName="ToggleBorder" Property="Background" Value="{StaticResource SelectionBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
